function handleDumpPump() {
  // TODO: реализовать логику обработчика
}

function processMessage(msg) {
  // TODO: реализовать обработку сообщения
}

let botInstance;

function setBot(bot) {
  botInstance = bot;
}

module.exports = {
  handleDumpPump,
  processMessage,
  setBot
};
